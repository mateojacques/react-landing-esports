import { createTheme } from '@material-ui/core/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#000',
    },
    secondary: {
      main: '#902DF2',
    },
    textPrimary: {
      main: '#fff',
    },
  },
})

export default theme
